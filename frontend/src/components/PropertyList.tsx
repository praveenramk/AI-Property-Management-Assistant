import React from 'react';

const PropertyList: React.FC = () => {
    return (
        <div>
            <h1>Property List</h1>
            {/* Add logic to fetch and display properties here */}
        </div>
    );
};

export default PropertyList;